---
layout: base.njk
title: IT Office Equipment
---

<section class="relative overflow-hidden bg-green-900 text-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center space-y-4">
    <p class="uppercase tracking-[0.3em] text-xs text-blue-200">IT Office Equipment</p>
    <h1 class="text-4xl font-bold">Peripherals & Upgrade Kits</h1>
    <p class="text-gray-200">Everything from NIC cards, laptops, mice, extenders, USB cables, printer cables, and more ready to boost your workspace.</p>
  </div>
</section>

<div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <a href="/products/" class="inline-flex items-center text-blue-600 font-semibold hover:text-blue-800 transition">&#8592; Back to Products</a>
</div>

<div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
  <div class="mb-8">
    <label class="block text-gray-700 font-semibold mb-2" for="office-search">Search office equipment</label>
    <div class="relative">
      <input id="office-search" type="text" placeholder="Type to filter devices..." class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500 filter-input" data-filter-list="office-card">
      <span class="absolute right-4 top-3 text-gray-400">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35M11 19a8 8 0 1 1 0-16 8 8 0 0 1 0 16z"/></svg>
      </span>
    </div>
  </div>
  <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
    {% set items = [
      {title:"Business Laptop", desc:"HP ProBook and OmniBook fleet ready for professional tasks.", img:"/assets/product/landing/laptop1.jpeg", link:"/products/it-office/laptops/"},
      {title:"Mouse and Extensions", desc:"Wireless and wired mice for every desk workflow.", img:"/assets/product/landing/Mouse.jpeg", link:"/products/it-office/mouse/"},
      {title:"Network Range Extension (Access Point)", desc:"Wi-Fi routers and long-range access points.", img:"/assets/product/landing/unifi1.jpeg", link:"/products/it-office/network/"},
      {title:"Computer Cables/Extensions", desc:"USB, HDMI, printer, and VGA cables plus adapters.", img:"/assets/product/landing/computer chargers.jpg", link:"/products/it-office/cables/"},
      {title:"Storage", desc:"External drives, flash disks, and portable memory.", img:"/assets/product/landing/ex disk.jpg", link:"/products/it-office/storage/"},
      {title:"Headset and Mic", desc:"Noise-cancelling headsets for collaboration and support desks.", img:"/assets/product/landing/headset.jpg", link:"/contact/"}
    ] %}
    {% for item in items %}
    {% set slug = item.title | slug %}
    <div id="item-{{ slug }}" class="bg-white rounded-2xl border border-gray-200 shadow-lg overflow-hidden flex flex-col office-card" data-search-entry data-search-title="{{ item.title }}" data-search-category="IT Office Equipment" data-search-description="{{ item.desc }}" data-search-url="{{ item.link }}">
      <img src="{{ item.img }}" alt="{{ item.title }}" class="h-48 w-full object-cover">
      <div class="p-6 space-y-3 flex-1 flex flex-col">
        <h2 class="text-xl font-semibold text-gray-900">{{ item.title }}</h2>
        <p class="text-gray-600 flex-1">{{ item.desc }}</p>
        <div class="pt-3">
          <a href="{{ item.link }}" class="inline-flex items-center text-blue-600 font-semibold hover:text-blue-800 transition">Details
            <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
          </a>
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const filterInputs = document.querySelectorAll('.filter-input');
    filterInputs.forEach(input => {
      const listClass = input.dataset.filterList;
      input.addEventListener('input', () => {
        const query = input.value.toLowerCase();
        document.querySelectorAll(`.${listClass}`).forEach(card => {
          card.style.display = card.innerText.toLowerCase().includes(query) ? '' : 'none';
        });
      });
    });
  });
</script>
